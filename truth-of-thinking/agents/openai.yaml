interface:
  display_name: "Truth of Thinking"
  short_description: "Define, classify, compare, cause — plus concept scenarios."
  default_prompt: "Use $truth-of-thinking on this question. If it is a single concept, run the compact full loop with use/avoid scenarios; if it is a scene, reason inside the scene."
